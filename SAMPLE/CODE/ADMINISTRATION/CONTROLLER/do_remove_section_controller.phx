// -- IMPORTS

import 'controller.php';
import '../../MODEL/section_model.php';

// -- TYPES

class DO_REMOVE_SECTION_CONTROLLER extends CONTROLLER
{
    method constructor(
        int section_id
        )
    {
        parent::constructor();

        RemoveDatabaseSectionById( section_id );

        Redirect( '/admin/section' );
    }
}

// -- STATEMENTS

var do_remove_section_controller = new DO_REMOVE_SECTION_CONTROLLER( var section_id );
